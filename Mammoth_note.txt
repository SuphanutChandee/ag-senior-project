Git command:
git checkout {another branch}
git checkout -b {new branch}

git add .
git commit -m "comment"
git push -u origin {branch}
git push

npm run start:dev 
#run backend http://localhost:3000/
npm start 
#run frontend http://localhost:2000/

npm run start:server

node ./src/PostNPut.js